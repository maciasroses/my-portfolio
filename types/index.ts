export interface NavbarProps {
    home: string,
    about: string,
    projects: string,
    contact: string,
    language: string
}

export interface HomeProps {
    title: string,
    button: string
}